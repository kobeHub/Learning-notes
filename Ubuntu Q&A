#1 解决python3 指向 python3.6 后无法打开gnome-terminal的问题
cd /usr/lib/python3/dist-package/gi/
ls 


sudo cp _gi.cpython-35m-x86_64-linux-gnu.so _gi.cpython-36m-x86_64-linux-gnu.so
sudo cp _gi_cairo.cpython-35m-x86_64-linux-gnu.so _gi_cairo.cpython-36m-x86_64-linux-gnu.so

#2 linux系统文件目录权限改变
可以用ls -al || ll 查看目录下所有文件的用户组权限每一个文件或者目录的访问权限（文件所有者权限．所有者同属组权限．系统中其他用户权限）都有三组可以用
chmod -（cfvr） [--help] [--version] mode file 
改变权限
必须参数：
-c 当发生改变时，报告处理信息
-f 错误信息不输出
-R 处理指定目录以及其子目录下的所有文件
-v 运行时显示详细处理信息
权限范围：
u ：目录或者文件的当前的用户
g ：目录或者文件的当前的群组
o ：除了目录或者文件的当前用户或群组之外的用户或者群组
a ：所有的用户及群组
权限代号：
r ：读权限，用数字4表示
w ：写权限，用数字2表示
x ：执行权限，用数字1表示
- ：删除权限，用数字0表示
s ：特殊权限 
chmod a+x file  添加所有用户可执行权限
chmod ug+x,o-x file 同组添加可执行，其他用户删除可执行
chmod u=x file  将所有者权限替换为可执行
chmod 755 file  等同于   chmod u=rwx,g=rx,o=rx file

git 创建新仓库并且绑定到ｇｉｔｈｕｂ：
1.不建立任何文件的github仓库
２．创建ｇｉｔ　添加远程仓库　　git remote add _name_ url
3.创建　README.md 并添加到本地仓库，　git push -u _name_ master
4.添加其他文件到本地仓库后即可使用　　git push   git pull
#############################
#  现有仓库对应：             #
# Hello-world   origin      #
# DataStrcture  data        #
#############################
